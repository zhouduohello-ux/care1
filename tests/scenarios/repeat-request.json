{
  "id": "repeat_request",
  "name": "L5 repeat request re-asks pending question",
  "description": "When a user replies 'repeat' to a pending question, the system re-asks the same question without treating it as a failed answer or clarification.",
  "user": {
    "phoneNumber": "test_repeat_request"
  },
  "script": [
    { "action": "reset" },
    { "action": "load_persona", "personaId": "controlled_asthma" },
    { "action": "advance", "to": "next_checkin" },
    { "action": "reply", "buttonId": "night_none" },
    { "action": "send", "text": "repeat" }
  ],
  "expectations": [
    {
      "afterStep": 5,
      "type": "message",
      "op": "not_contains",
      "value": "didn't catch that",
      "caseInsensitive": true,
      "description": "Repeat request is not treated as a reprompt"
    },
    {
      "afterStep": 5,
      "type": "message",
      "op": "contains",
      "value": "reliever",
      "caseInsensitive": true,
      "description": "Repeat request re-asks the pending reliever question"
    }
  ]
}
